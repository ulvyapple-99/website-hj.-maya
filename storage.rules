
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // PUBLIK: Boleh lihat gambar
      allow read: if true;
      
      // ADMIN: Hanya boleh upload gambar jika login
      allow write: if request.auth != null;
    }
  }
}
